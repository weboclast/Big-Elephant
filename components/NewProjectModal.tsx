// This component is no longer used in the primary workflow.
// Project creation is now handled by the chat interface on the HomeScreen.
// The file is kept to avoid breaking potential future references, but its functionality is removed.
const NewProjectModal = () => null;
export default NewProjectModal;
